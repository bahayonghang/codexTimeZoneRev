# Flutter + Forui 重制执行方案

此文档保留迁移时的设计与验收记录；当前使用与发布方式以 [项目简介](README.md) 为准。

## 目标与边界

- 分支：`codex/flutter-forui-rebuild`。
- Windows 和 macOS 桌面重制，业务功能与现有版本保持一致。
- 所有应用内交互控件使用 Forui；Flutter 基础布局、文字、语义及系统文件选择器除外。
- 保留用户配置。按用户后续指令清理旧 Vue/Tauri 工程，并经用户明确确认删除根目录旧 EXE；功能所需原生后端和图标迁入新工程。
- 开发工具由开发者自行安装；本地缓存与构建产物不提交。
- 本方案不包含发布、推送远端、修改系统时区或操作用户正在运行的 Codex。

## 基线与版本

迁移前基线为 Vue 3 + MacVue + Tauri 2；原 `resource/backend` 承载 Rust 公共及平台逻辑。现已迁入 `native/launcher_core/src/platform`。原 IP 查询行为已迁入 Dart 服务。

本轮核对官方发布信息，选用 Flutter 3.47.5 / Dart 3.13.4、Forui 0.27.0。提交应用 lockfile 固定解析结果。Flutter 下载后验证官方 SHA-256。

## 目录与依赖方向

```text
flutter_app/                  Flutter 桌面应用、平台 runner、测试
  lib/app/                    应用壳、主题与页面
  lib/domain/                 设置、时区等不可变模型
  lib/services/               FFI、时间、网络、本地偏好
  lib/state/                  草稿与提交状态控制
native/launcher_core/         独立 Rust cdylib，src/platform 内含公共及平台逻辑
scripts/                     开发、测试、原生库打包脚本
```

依赖方向：Forui 界面 → Dart controller/service → Rust JSON C ABI → 平台实现。原生模块已迁入 native，构建不依赖旧工程目录。

FFI 采用带协议版本的 JSON 请求/响应，显式分配及释放 UTF-8 字符串。所有耗时调用在后台 isolate 执行；Rust 命令互斥串行，捕获可恢复 panic，统一错误封装。原生库随应用发布，配置目录继续基于主应用路径解析。

## 功能与验收矩阵

| 功能 | 实施要求 | 验收场景 |
| --- | --- | --- |
| 初始化 | 读取旧配置、时区目录、自动发现路径 | 首次运行、有效配置、损坏配置、重试；失败不得覆盖配置 |
| 草稿 | 当前值与最后提交快照分离 | 修改、恢复原值、保存失败、操作中禁用、重复点击 |
| 地区时区 | 使用后端支持集合，Dart IANA 数据计算预览 | 上海、纽约夏令时边界、加尔各答、加德满都、非法标识 |
| 固定偏移 | 保持 -12…+14 整小时范围 | 零、正负端点、跨日；Etc/GMT 符号反向 |
| 路径 | 空值自动发现，系统选择器，后端最终校验 | exe/app、空格及中文路径、错误文件、取消选择 |
| 启动 | 提交当前草稿后启动，仅对子进程注入 TZ | 已运行拒绝、提前退出、参数和环境变量正确 |
| 快捷方式 | 指向新版主程序；保留平台原有规则 | Windows lnk、macOS app 链接、不覆盖不相关文件 |
| Dream Skin | 开关保存与打开管理工具分开 | 工具缺失、端口占用、启动及应用超时、兼容开/关 |
| 网络 | 国内/国际独立，多源竞速，失败隔离与取消 | 双成功、单成功、全部失败、缓存恢复、手动刷新 |
| 代理 | 验证 Flutter HTTP 与旧 WebView 出口行为 | 无代理、系统代理、代理不可用；必要时原生适配 |
| 复制 | 系统剪贴板写入后读回校验 | 成功、失败、短暂反馈恢复 |
| 应用识别时区 | 只改变草稿，按支持集合校验 | 可用地区、不支持地区、未保存状态 |
| 外观 | system/light/dark 持久化 | 系统主题变化、重启恢复、弹层主题一致 |
| 生命周期 | 窗口隐藏时停止时钟更新 | 最小化、恢复立即刷新、不泄漏计时器 |

旧 `settings.json` 保持位置和格式。WebView localStorage 与 Flutter 本地存储分离：业务配置必须兼容；主题及 IP 缓存的一次性迁移另行验证，无法可靠迁移时在发布说明明确主题重置/缓存重新查询，不读取或修改不相关浏览器数据。

## 界面规范

- FScaffold 页面骨架；顶部标题和主题选择；主区域设置 + 时间预览；下方双线路网络卡片；底部固定操作栏。
- FCard / FDivider 分组，FSelect 选择时区和偏移，FTextField 路径，FSwitch 兼容开关，FButton 操作，FAlert / FBadge / FProgress / FToast 反馈。
- 颜色、字体、间距、圆角统一使用 FTheme，桌面密度，不使用 Material/Cupertino 可见控件。
- 宽窗口双列、窄窗口单列；长路径/IPv6/组织名称换行或可选中文本；底栏允许换行。
- 验收尺寸 1280×900、760×900、600×800；浅深主题、键盘 Tab/Enter/Escape、焦点可见、中文及缩放。

## 实施阶段与完成门槛

### P0 方案与环境
- [x] 检查仓库与现有未提交改动，创建专用分支。
- [x] 完成功能基线、迁移方案和验收矩阵。
- [x] 安装并校验 Flutter，核对 Windows C++ 桌面工具链。

### P1 Rust 核心与桥接
- [x] 创建独立 cdylib，复用既有后端，不依赖 Tauri。
- [x] 实现 ABI 版本、JSON envelope、字符串释放、错误和互斥保护。
- [x] 测试无效请求、未知命令、时区边界与内存调用契约。
- [x] Dart 后台 isolate 调用；缺少原生库时展示可恢复错误。
- 门槛：Rust 测试通过；Windows Flutter 能通过桥接读取设置及目录。

### P2 Flutter + Forui 完整页面
- [x] 创建 Windows/macOS runner，锁定依赖。
- [x] 模型、状态控制器、Forui 主题与响应式页面。
- [x] 加入安全演示模式，固定数据，禁止真实系统操作。
- [x] 浅深主题、多尺寸 widget 测试及截图验收。
- 门槛：flutter analyze 和界面测试通过，无布局溢出，无非 Forui 可见控件。

### P3 等价功能接入
- [x] 原生初始化、路径校验、保存、启动、快捷方式与 Dream Skin 代码接入（真实系统副作用仍待实机验收）。
- [x] IANA 时间与生命周期、双线路网络、缓存、主题与剪贴板代码接入。
- [x] 针对状态机、夏令时边界、网络失败隔离的行为测试。
- [x] Windows 静态代理/PAC 接入及本地代理测试；macOS URLSession 通道实现。
- [x] 隔离客户端启动、时区注入、配置读写保护、临时快捷方式验收；文件选择/剪贴板 UI 自动化测试。
- [ ] 与旧版实际代理出口对比，文件选择器与剪贴板实机验证，实际 Codex/Dream Skin 联动。
- 门槛：矩阵中的可自动化项目通过，系统副作用在隔离测试/用户明确安排下验证。

### P4 构建与交付
- [x] 开发/构建脚本自动编译并捆绑原生库；Windows release 目录打包为 ZIP。
- [ ] macOS dylib/app 打包、签名与权限验证（须 macOS 实机/CI）。
- [x] 更新使用说明、环境版本、已知限制与验收记录。
- [x] 按用户后续要求提前清理旧 UI、旧 CI 与 Node/Tauri 构建入口；更新 Flutter 默认文档。此项不代表 macOS/实际客户端验收已完成。
- 门槛：目标平台实机验证；未验证项目明确标记，不将构建通过等同于真实启动通过。

## 验证命令与顺序

1. `cargo test --manifest-path native/launcher_core/Cargo.toml`。
2. Flutter `pub get`、`analyze`、`test`。
3. Windows release 构建，验证库和资源齐备。
4. 演示模式截图、正式模式只读初始化；真实启动/快捷方式/Dream Skin 在不会影响正在使用的客户端时验证。
5. macOS 环境执行同等验证，记录与 Windows 分开的结果。

## 风险及处理

- Forui 尚为 0.x：精确锁定版本，基于已安装源码确认 API，不边迁移边升级。
- 原生桥接内存/线程：分配与释放必须同库完成；串行后台执行；禁用重复提交。
- 双工具链：脚本提供明确 SDK 和 Rust 检查，失败停止，不产生看似完整的产物。
- Windows C++ 组件不足：安装 Visual Studio C++ 桌面工具链并按需补齐组件。
- macOS 无法在 Windows 实测：交付标明待验证，保留旧版作为回退。

## 执行记录

- 2026-09-22：已建立分支和方案；开始安装 Flutter SDK、实现独立 Rust 桥接。
- 2026-09-22：Flutter 3.47.5 的官方 SHA-256 校验通过；Windows C++ 工具链满足桌面构建。
- 2026-09-22：Rust 5 项测试通过；Flutter 16 项测试通过；analyze 无问题；浅深主题三种尺寸截图通过。
- 2026-09-22：Windows Release + 便携 ZIP 构建通过；Dart → DLL 实测发现客户端和 10 次中文错误返回/释放。未执行保存、启动、快捷方式及 Dream Skin 系统操作。
- 2026-09-22（第二轮）：系统代理接入、取消/超时保护、文件选择与剪贴板状态反馈完成；Rust 10 项、Flutter 29 项测试通过，analyze 无问题。
- 2026-09-22（第二轮）：隔离原生验收完成配置兼容与写入保护、中文路径启动、子进程 TZ、环境变量清理、重复启动拦截及损坏配置恢复；快捷方式仅在临时目录验证，未影响用户桌面。
- 2026-09-22（第二轮）：加入 MSVC 运行库打包、macOS 产品名一致性修正和手动双平台 CI。macOS 构建与远端 CI 尚未执行。
- 2026-09-22（第二轮）：Windows Release 重建成功；ZIP 内 25 个文件通过资源/运行库检查且不含用户设置。解压包演示进程启动并持续运行，包内 DLL 只读集成检查通过。
- 当前交付仍为开发版本，P3 真实客户端联动及 P4 双平台发布门槛尚未全部达成。后续验收：实际代理出口对比及 SOCKS/认证代理兼容性 → 实际 Codex/Dream Skin 联动 → macOS 构建与回归 → 默认入口切换。
- 2026-09-22（清理轮）：应用标题区、Windows EXE 与 macOS 资源沿用原图标；Rust 后端迁入 native，删除旧 Vue/Tauri/Node 源码、旧构建工作流及经确认的旧 EXE。清理后 Rust 10 项与 Flutter 29 项测试通过，静态检查无问题；README 已切换为 Flutter 默认说明。
- 2026-09-22（清理轮）：Windows Release 和便携 ZIP 重建成功；26 个打包文件含图标及运行库、不含用户设置。解压后演示启动及 DLL 只读检查通过，浅深主题三尺寸图标布局截图通过。ZIP SHA-256：`2257DD9A0CD5DACED2CF9F223C47C99CD5959A83239A8E2579D7162C3053B992`。
- 2026-09-24：Apple Silicon Mac 上完成 Flutter 测试、应用构建、临时签名和 Dream Skin 重新注入验证；发布版改由 GitHub Release 触发云端双平台构建。
